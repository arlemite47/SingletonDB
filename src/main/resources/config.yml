database:
  enabled: true
  type: "mysql" # mysql / postgres
  host: "localhost"
  port: 3306
  database: "minecraft"
  user: "root"
  password: "password"

update-interval-seconds: 300

messages:
  prefix: "&6[TonStats] "
